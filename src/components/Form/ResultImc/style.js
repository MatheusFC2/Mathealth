import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    ResultImc: {
        flex: 1,
        marginTop: 15,
        paddingTop: 60,
        borderRadius: 50,
        alignItems: "center",
        width: "100%",
    },
    numerImc: {
        fontSize: 48,
        color: "#008000",
        fontWeight: "bold",
    },

    information: {
        fontSize: 18,
        color: "#008000",
        fontWeight: "bold",
    }
})

export default styles